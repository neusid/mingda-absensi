import 'package:flutter/cupertino.dart';

class AppShadows {
  static const BoxShadow shadow094 = BoxShadow(
    color: Color(0xff0000000A),
    spreadRadius: 0,
    blurRadius: 9,
    offset: const Offset(0, 0),
  );
}
